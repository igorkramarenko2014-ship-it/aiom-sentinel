# Contributing

Keep changes bounded to the approved phase. Add tests for malformed and boundary inputs, run every
command in `docs/VERIFICATION.md`, and update the evidence contract for schema changes. Fixtures must
be harmless synthetic data. Commits must not add telemetry, remediation, privileged collectors,
malware, or offensive payloads.
