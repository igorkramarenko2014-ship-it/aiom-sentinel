# Benchmarks

Executable Criterion benchmarks live beside the measured crate at
`crates/sentinel-scanner/benches/scanner.rs`. Run all benchmark compile gates with
`cargo bench --workspace --no-run`; execute the scanner benchmark with
`cargo bench -p sentinel-scanner`.
