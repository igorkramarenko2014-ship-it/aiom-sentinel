# Network Policy Contract

implementation_authority: NONE
current_runtime_status: DESIGN_ONLY

Future adapters may use WFP, nftables, or Network Extension only with explicit authority. Network
visibility and enforcement are separate. Rules are deterministic, versioned, attributable, reversible,
audited, capability-aware, and never directly mutated by AI. No packet driver is authorized.
