# Rule Bundle Content Identity Contract
implementation_authority: NONE
current_runtime_status: DESIGN_ONLY
`bundle_content_id` uses a named content domain and preimage. `transport_zip_sha256` identifies transport bytes only and is forbidden as a content-ID input or alias.
