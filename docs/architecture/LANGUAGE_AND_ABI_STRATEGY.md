# Language and ABI Strategy
implementation_authority: NONE
current_runtime_status: DESIGN_ONLY
Rust portable core ↔ authenticated versioned IPC or minimal stable C ABI ↔ C adapter or C++ hidden behind C ABI. No public C++ ABI or scanner clone.
