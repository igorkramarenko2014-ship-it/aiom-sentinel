# Local-First Data Flow
implementation_authority: NONE
current_runtime_status: DESIGN_ONLY
Rules, models, and policies flow down. Bounded evidence summaries flow up. Raw files stay local by default; endpoint scans without continuous server access.
